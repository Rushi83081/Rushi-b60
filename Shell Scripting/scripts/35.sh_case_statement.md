# 🐚 Case Statement – Example 1

📌 **Example 1: Case Statement Script**

This script demonstrates the use of a **case statement** in shell scripting to execute different commands based on user input.

---

## 🧾 Script Code

```bash
#!/bin/bash

echo "Provide an option:"
echo "a for print date"
echo "b for script ls"
echo "c to check the current location"

read choice

case $choice in
    a) date ;;
    b) ls ;;
    c) pwd ;;
    *) echo "Enter valid input" ;;

esac
```
