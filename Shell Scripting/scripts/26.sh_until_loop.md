# until Loop
## Description

- An until loop runs until the condition becomes true (opposite of while loop).

## Example 1: Countdown
```
#!/bin/bash

a=10

until [ $a -eq 0 ]
 do
   echo "Value is $a"
   a=$((a-1))
 done
```

**What this example does:**

- Prints values from 10 down to 1.
