## 4. Reading a File (9.sh)

 * make a file demo.txt and add 3-4 lines 
```
cat > demo.txt
```

### Example
```
file_nm="demo.txt"
filecontents=$(cat "$file_nm")

echo "Your file contents are:"
echo "$filecontents"
```
* $(...) is command substitution.

* It stores command output into a variable.

## 5. Command Substitution
```
variable=$(command)
```
* **$(...)** is used to store output of a command into a variable.
