## Filter users with UID greater than 1000:

## Example (15.sh)

```
#!/bin/bash

awk -F: '$3 >= 1000 { print $1 }' /etc/passwd
```
```
bash 15.sh
```

**👉 It lists all normal (non-system) users on the Linux system.**
