# Root User Check Script

## Example (18.sh)
```
#!/bin/bash

if [ $(id -u) -ne 0 ]
then
    echo "You must run this script as root"
else
    echo "You are root, script will continue"
fi
```
```
bash 18.sh
```

- Checks whether the script is executed by the root user and stops execution if not.
