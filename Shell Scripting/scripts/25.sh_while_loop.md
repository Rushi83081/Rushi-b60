# While Loop

## Example
```
#!/bin/bash

count=1

while [ $count -le 5 ]
do
  touch app_log_$count.log
  echo "Log file $count created"
  count=$((count+1))
done
```

Creates 5 log files (app_log_1.log to app_log_5.log) using a while loop and prints a confirmation message for each file.
