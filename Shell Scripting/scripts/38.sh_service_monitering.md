# 🛠️ Service Monitoring Script (httpd)

## 📄 File
```
vim 38.sh
```

## 🔹 Script Logic

```
#!/bin/bash

service_name="httpd"

if systemctl is-active "$service_name" --quiet
then
    echo "$service_name is running"
else
    echo "$service_name is NOT running"
    echo "Attempting to restart"
    sudo systemctl restart "$service_name" --quiet


    if systemctl is-active "$service_name" --quiet
    then
        echo "$service_name restarted successfully"
    else
        echo "Failed to restart $service_name"
        echo "Manual intervention required"
    fi
fi
```
## 📌 Purpose

-  Checks if httpd service is running

-  Restarts service automatically if stopped

-  Notifies if manual intervention is required
