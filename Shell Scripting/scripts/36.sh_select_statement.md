# 🐚 Select with Case Statement – Example 2

📌 **Example 2: Select with Case Statement**

This script demonstrates how to use the **select statement with a case statement** to create a **menu-driven program** in shell scripting.

---

## 🧾 Script Code

```bash
#!/bin/bash

PS3="Enter your choice: "

select option in "start server" "stop server" "restart server" "exit"
do
    case $option in
        "start server")
            echo "Server started"
            ;;
        "stop server")
            echo "Server stopped"
            ;;
        "restart server")
            echo "Server restarted"
            ;;
        "exit")
            echo "Exiting"
            break
            ;;
        *)
            echo "Invalid choice"
            ;;
    esac
done
```
---
## 🧠 How It Works

  * 📋 select automatically creates a numbered menu

  * 🧾 PS3 customizes the prompt message

  * ⌨️ User selects an option by number

  * 🔀 case matches the selected option

  * 🔁 Menu repeats until exit is chosen

  * 🛑 break stops the loop

---

## ⭐ Key Concepts Used

  - select → Menu creation

  - case → Decision making

  - PS3 → Prompt string

  - break → Exit loop
